DevLog 8 做完审查，也记下了 SPACE 的性能问题。DevLog 9 把 Architecture V2 迁完并推上 main：根应用先做 App Shell 和桌面/移动分流；桌面用真实 URL 连 SPACE、作品、Profile、DevStories，同一套 Canvas / Rapier / GLB / 玩家位置会一直复用，回展馆不用再补左键。渲染是 WebGPU 优先、WebGL2 回退，完整 / 简化两档都留着雾和色彩层级；启动由 attempt-scoped boot controller 管，旧异步结果盖不掉新重试。

旧 Splash 和彩蛋换成了桌面 3D StartLobby、移动端纯 DOM MobileStartMenu。桌面入口大约 50 条展品 title / subtitle 弹幕，鼠标附近有非线性“黑洞”力场，字符会旋、会惯性偏移，碰到指针就碎成白点（最大形变 30px，点半径最大 3px）。Focus 只在选中作品后加载该作品全部图片并显示进度，不预取冷媒体。另外建了视觉规范、资产清单、下载 / 解码 / GPU / 帧预算、浏览器基线和 release gates；GLB、Blender、源资产这轮没动。`npm run verify:quick` 和 `npm run build:chunks` 过了，合并点在 `main` / `origin/main` 的 `343d0e8`。本日志单独作为内容提交交接。
