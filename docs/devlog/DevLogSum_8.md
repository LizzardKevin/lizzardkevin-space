做了一轮 SPACE 项目管理收口：先在独立分支上用三个并行审查方向看 repo hygiene、SPACE runtime 和 content/devlog 数据，再把低风险问题直接修掉，包括 root ignore 不再误伤 `apps/web/tests/release`、placement cache 去掉时间戳、Blender 脚本补跨平台 PATH 查找、GitHub bootstrap 不再默认推已有 origin、Focus 图片/视频页暂停隐藏模型 Canvas 渲染、scene exhibit clone 卸载时释放自有 material，以及 mobile / daily resume 的 localStorage 安全降级；同时更新 README、DevStories 维护说明和 DevLog 7 里的过期描述。新增 DevLog 8 完整版和 summary，并把网页端 DevStories 1 到 8 条整理成更适合继续阅读和维护的第一人称记录；最新部分明确记录 SPACE 现在遇到严重性能问题，下一轮需要单独判断主开销来自 UABB、展品模型、投影图片、后处理还是主场景。本轮用 targeted contracts、`npm run verify:quick` 和 `npm run build:chunks` 验证，不 push，也不做 GitHub Pages 迁移。
