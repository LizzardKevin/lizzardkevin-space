修复 SPACE 移动手感排障链路：新增 dev-only 移动 debug overlay 与 collider handle 到 `COL_*` 名称反查，能在真实浏览器里看到位置、实际/期望速度、ratio、grounded 和当前接触体；保守回退自定义 pointer lock controls 到 Drei，优先恢复第一人称视角可用性；确认所谓“服务器/Vite 动态 tick”并非 Vite server 驱动物理，而是浏览器端 R3F render `dt` 与 Rapier fixed `1/60` physics step 混用，随后显式固定 `<Physics timeStep={1 / 60}>` 并让 `PlayerController` 使用同一固定 timestep；修复后将移动速度从临时翻倍值回到已确认的 2.45 / 3.85 基线，并用 SPACE interaction contract、TypeScript、lint、build 和 Vite 5173 本地服务验证。
