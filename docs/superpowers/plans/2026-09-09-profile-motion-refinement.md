# Profile motion refinement

User-authorized follow-up to the existing Profile particle narrative. Execute in the current worktree without committing or publishing.

1. Reduce chapter minimum length from 2 to 1.2 viewports. Replace half-viewport plateaus with 0.12-viewport holds and quintic easing. Scatter fully before switching model identity, with a short fully dispersed midpoint; reverse scrolling must retrace exactly.
2. Reuse WorkDetail's spacing-based pointer displacement amplitude, random directions, 2.1 Hz-phase flutter, and Gaussian influence. Preserve current half-angle camera parallax.
3. Vary each column's phase speed smoothly using seeded group parameters. Height range 12–130% of authored height, anchored at ground. Share the formula between shader and projected title anchors.
4. Add a bounded three-slot noninteractive title overlay. Select actual chapter work names from localized Profile content. Trigger on rising columns, last 3–8 seconds, flicker through ASCII, follow projected column positions, and disappear outside architecture chapters. Test bounded lifetime, uniqueness, and catalog membership.
5. Rebuild only DUMBO supplemental points for a second-floor street level and coherent oblique bridge axis. Apply current-eye facade ray occlusion in addition to the existing window portal.
6. Reuse WorkEdgeNav's flicker loop for localized archive destinations, preserving work previous/next labels.
7. Validate focused animation/catalog/geometry tests, quick verification, builds and protected release; inspect live Profile and archive links, both render backends. Preserve raw Rhino, Work renderer, generated content and mobile sources.
